kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 6443   # inside Kind
    hostPort: 6443        # exposed on host
    protocol: TCP
